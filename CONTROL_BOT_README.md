# Telegram Bot для управления автоматизацией April Shipments

Полноценная система управления автоматизацией бронирования перевозок через Telegram бот.

## 📋 Содержание

- [Описание](#описание)
- [Возможности](#возможности)
- [Архитектура](#архитектура)
- [Требования](#требования)
- [Установка](#установка)
- [Настройка](#настройка)
- [Использование](#использование)
- [Режимы работы](#режимы-работы)
- [Структура проекта](#структура-проекта)
- [Troubleshooting](#troubleshooting)

## 🎯 Описание

Система состоит из двух частей:

1. **Control Bot** - Telegram бот для управления автоматизацией
2. **Automation Scripts** - Скрипты автоматизации (Режим 1 и Режим 2)

Control Bot предоставляет удобный интерфейс для:
- Управления несколькими сессиями автоматизации
- Переключения между режимами работы
- Мониторинга состояния в реальном времени
- Просмотра статистики и логов

## ✨ Возможности

### 🔐 Система авторизации
- Авторизация только по Telegram User ID
- База данных разрешенных пользователей
- Логирование всех попыток доступа
- Безопасное хранение данных

### 📊 Управление сессиями
- Поддержка нескольких аккаунтов Telegram
- Запуск/остановка/перезагрузка сессий
- Переключение между режимами работы
- Мониторинг статуса процессов

### 📈 Мониторинг и статистика
- Статус всех сессий в реальном времени
- Счетчики триггеров, кнопок и ошибок
- Время работы (uptime)
- Просмотр логов прямо в Telegram

### 🎮 Два режима работы

#### Режим 1: Одна кнопка (Быстрый)
- Нажатие ТОЛЬКО кнопки "Список прямых перевозок"
- Максимальная скорость (0 секунд задержки)
- Используется `simple_button_automation.py`

#### Режим 2: Три кнопки (Полная автоматизация)
- Полная последовательность действий
- Нажатие всех кнопок по порядку
- Используется `bot_automation.py`

## 🏗 Архитектура

```
┌─────────────────────────────────────────────────────────┐
│                    Пользователь                          │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│                  Control Bot                             │
│              (control_bot.py)                            │
│  - Авторизация пользователей                             │
│  - Интерфейс управления                                  │
│  - Обработка команд                                      │
└────────────┬───────────────────────┬────────────────────┘
             │                       │
             ▼                       ▼
┌────────────────────────┐  ┌─────────────────────────────┐
│   Session Manager      │  │       Database              │
│  (session_manager.py)  │  │     (database.py)           │
│  - Запуск процессов    │  │  - SQLite база              │
│  - Остановка           │  │  - Таблицы: users,          │
│  - Мониторинг          │  │    sessions, access_logs    │
│  - Парсинг логов       │  │                             │
└────────────┬───────────┘  └─────────────────────────────┘
             │
             ├───────────────────┬─────────────────────────┐
             ▼                   ▼                         ▼
┌──────────────────────┐ ┌──────────────────────┐  ┌────────────┐
│      Режим 1         │ │      Режим 2         │  │   Логи     │
│ simple_button_       │ │  bot_automation.py   │  │ logs/*.log │
│ automation.py        │ │                      │  └────────────┘
│ (Одна кнопка)        │ │ (Три кнопки)         │
└──────────────────────┘ └──────────────────────┘
             │                   │
             └───────────┬───────┘
                         ▼
                ┌─────────────────┐
                │  @ACarriers_bot │
                └─────────────────┘
```

## 📦 Требования

- Python 3.9+
- Telegram API credentials (api_id, api_hash)
- Telegram Bot Token (от @BotFather)
- Аккаунт Telegram для автоматизации

## 🚀 Установка

### 1. Клонирование репозитория

```bash
git clone <repository_url>
cd april2
```

### 2. Создание виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

## ⚙️ Настройка

### 1. Получение Telegram API credentials

1. Перейдите на https://my.telegram.org/apps
2. Войдите со своим номером телефона
3. Создайте новое приложение
4. Скопируйте `api_id` и `api_hash`

### 2. Создание Control Bot

1. Найдите @BotFather в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен бота

### 3. Настройка .env файла

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Заполните переменные:

```env
# Токен Control Bot (от @BotFather)
CONTROL_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# Telegram API (от my.telegram.org)
API_ID=12345678
API_HASH=abcdef1234567890abcdef1234567890

# Номер телефона для автоматизации
PHONE_NUMBER=+79512586335

# Username бота для бронирования
BOT_USERNAME=@ACarriers_bot

# Имя файла сессии
SESSION_NAME=telegram_session
```

### 4. Инициализация базы данных

Запустите скрипт инициализации:

```bash
python setup_db.py
```

Следуйте инструкциям:
1. Введите ваш Telegram User ID (получите у @userinfobot)
2. Добавьте первую сессию автоматизации

### 5. Создание файла сессии Telegram

Если вы добавили сессию в БД, создайте файл сессии:

```bash
python simple_button_automation.py \
  12345678 \
  "your_api_hash" \
  "+79512586335" \
  "sessions/session1.session" \
  "@ACarriers_bot" \
  1
```

При первом запуске:
1. Telegram попросит код подтверждения (отправит на телефон)
2. Введите код
3. Если включена двухфакторная аутентификация - введите пароль
4. Файл сессии будет создан

После создания сессии можно остановить скрипт (Ctrl+C).

## 🎮 Использование

### Запуск Control Bot

```bash
python control_bot.py
```

Бот будет запущен и ожидать команд в Telegram.

### Основные команды

- `/start` - Главное меню
- `/status` - Быстрый статус всех сессий
- `/sessions` - Список сессий
- `/help` - Справка

### Интерфейс бота

#### Главное меню

```
╔════════════════════════════════════════════╗
║  УПРАВЛЕНИЕ АВТОМАТИЗАЦИЕЙ APRIL SHIPMENTS  ║
╚════════════════════════════════════════════╝

Состояние системы:
Сессия 1: 🟢 [RUNNING] - Режим 1 (1 кнопка)

[  УПРАВЛЕНИЕ СЕССИЯМИ  ]
[   МОНИТОРИНГ И ЛОГИ   ]
[      НАСТРОЙКИ        ]
[        ПОМОЩЬ         ]
```

#### Управление сессией

```
╔════════════════════════════════════════════╗
║  СЕССИЯ 1: +7951***6335                    ║
║  Статус: 🟢 RUNNING                        ║
║  Режим: Режим 1 (1 кнопка)                 ║
║  Время работы: 02:15:33                    ║
╚════════════════════════════════════════════╝

[    ОСТАНОВИТЬ    ]
[  ПЕРЕЗАГРУЗИТЬ   ]

[  РЕЖИМ 1: 1 кнопка  ] [✓]
[  РЕЖИМ 2: 3 кнопки  ] [ ]

[   СТАТИСТИКА      ]
[   ПРОСМОТР ЛОГОВ  ]
```

### Добавление пользователей

Для добавления новых пользователей используйте Python:

```python
from database import get_database

db = get_database()
db.add_user(
    user_id=123456789,  # Telegram User ID
    username="john_doe",
    first_name="John"
)
```

Или создайте скрипт:

```bash
python -c "from database import get_database; db = get_database(); db.add_user(123456789, 'username', 'Name')"
```

### Добавление новых сессий

Через Python:

```python
from database import get_database

db = get_database()
session_id = db.add_session(
    session_name="Аккаунт 2",
    phone_number="+79001234567",
    api_id=12345678,
    api_hash="your_api_hash",
    session_file="sessions/session2.session"
)
```

## 🔧 Режимы работы

### Режим 1: Одна кнопка (Быстрый)

**Описание:**
- При появлении сообщения "Появились новые перевозки"
- Нажимается ТОЛЬКО кнопка "Список прямых перевозок"
- Максимальная скорость реакции (0 секунд задержки)

**Файл:** `simple_button_automation.py`

**Параметры:**
- `TRIGGER_MESSAGE` = "Появились новые перевозки"
- `TARGET_BUTTON_TEXT` = "Список прямых перевозок"
- `DELAY_AFTER_TRIGGER` = 0.0

**Использование:**
Выберите в интерфейсе бота: **[Режим 1: 1 кнопка]**

### Режим 2: Три кнопки (Полная автоматизация)

**Описание:**
- Шаг 1: Нажатие "Список прямых перевозок"
- Шаг 2: Выбор первой перевозки из списка
- Шаг 3: Нажатие кнопки подтверждения/бронирования

**Файл:** `bot_automation.py`

**Параметры (в config.py):**
- `BUTTON_STRATEGY` = "first" (или "custom", "all")
- `DELAY_AFTER_TRIGGER` = 0.05
- `DELAY_BETWEEN_CLICKS` = 0.15
- `AUTOMATION_TIMEOUT` = 3.0

**Использование:**
Выберите в интерфейсе бота: **[Режим 2: 3 кнопки]**

### Переключение режимов

1. Откройте управление нужной сессией
2. Нажмите на желаемый режим
3. Подтвердите перезапуск сессии
4. Бот автоматически:
   - Остановит текущий процесс
   - Изменит режим в БД
   - Запустит новый процесс

## 📁 Структура проекта

```
april2/
├── control_bot.py              # Главный бот управления
├── database.py                 # Работа с БД SQLite
├── session_manager.py          # Управление процессами
├── config.py                   # Конфигурация
├── setup_db.py                 # Инициализация БД
│
├── bot_automation.py           # Режим 2 (3 кнопки)
├── simple_button_automation.py # Режим 1 (1 кнопка)
│
├── .env                        # Переменные окружения (не в git)
├── .env.example                # Пример .env
├── requirements.txt            # Зависимости Python
├── README.md                   # Основной README
├── CONTROL_BOT_README.md       # Этот файл
│
├── logs/                       # Логи автоматизации
│   ├── automation_1_2025-11-12.log
│   ├── automation_2_2025-11-12.log
│   └── control_bot.log
│
├── sessions/                   # Файлы сессий Telegram
│   ├── session1.session
│   └── session2.session
│
└── control_bot.db              # База данных SQLite
```

## 🐛 Troubleshooting

### Проблема: Бот не отвечает

**Решение:**
1. Проверьте, запущен ли `control_bot.py`
2. Проверьте токен в `.env`
3. Проверьте логи: `logs/control_bot.log`

### Проблема: "Доступ запрещен"

**Решение:**
1. Получите ваш User ID у @userinfobot
2. Добавьте себя в БД:
   ```bash
   python setup_db.py
   ```
   или через Python:
   ```python
   from database import get_database
   db = get_database()
   db.add_user(YOUR_USER_ID, "username", "Name")
   ```

### Проблема: Сессия не запускается

**Решение:**
1. Проверьте, создан ли файл сессии Telegram
2. Проверьте правильность API_ID и API_HASH
3. Проверьте логи сессии: `logs/automation_X_DATE.log`
4. Проверьте статус процесса:
   ```bash
   ps aux | grep automation
   ```

### Проблема: "Процесс не запущен", но в БД статус "running"

**Решение:**
1. Откройте мониторинг в боте
2. Нажмите "Обновить" - статус автоматически обновится
3. Или перезапустите сессию

### Проблема: Ошибка импорта модулей

**Решение:**
```bash
# Переустановите зависимости
pip install --upgrade -r requirements.txt

# Проверьте версию Python
python --version  # Должна быть 3.9+
```

### Проблема: Сессия Telegram не авторизуется

**Решение:**
1. Удалите старый файл сессии
2. Создайте новую сессию:
   ```bash
   python simple_button_automation.py <args>
   ```
3. Введите код из SMS
4. При необходимости - пароль 2FA

## 📊 Мониторинг

### Просмотр статистики

В интерфейсе бота:
1. Выберите сессию
2. Нажмите "Статистика"
3. Увидите:
   - Триггеров обнаружено
   - Кнопок нажато
   - Ошибок
   - Последний триггер
   - Последнее действие

### Просмотр логов

В интерфейсе бота:
1. Выберите сессию
2. Нажмите "Логи"
3. Выберите количество строк (20, 50, 100, 200)

Или напрямую:
```bash
tail -f logs/automation_1_2025-11-12.log
```

### Системные логи Control Bot

```bash
tail -f logs/control_bot.log
```

## 🔒 Безопасность

1. **Не коммитьте .env файл** - он содержит секретные ключи
2. **Храните токены в безопасности** - не передавайте их третьим лицам
3. **Регулярно проверяйте логи доступа**:
   ```python
   from database import get_database
   db = get_database()
   logs = db.get_access_logs(limit=100)
   for log in logs:
       print(log)
   ```
4. **Используйте отдельные аккаунты** для автоматизации
5. **Номера телефонов маскируются** в интерфейсе бота

## 📝 Логи

### Формат логов автоматизации

```
2025-11-12 14:32:15 - __main__ - INFO - 🚨 ТРИГГЕР ОБНАРУЖЕН! (#47)
2025-11-12 14:32:15 - __main__ - INFO - ⚡ Нажатие кнопки: 'Список прямых перевозок'
2025-11-12 14:32:15 - __main__ - INFO - ✓ Кнопка успешно нажата
2025-11-12 14:32:15 - __main__ - INFO - 📊 Статистика: Триггеров: 47, Кнопок: 47, Ошибок: 2
```

### Ротация логов

- Каждый день создается новый файл
- Формат имени: `automation_{session_id}_{date}.log`
- Старые логи можно удалять вручную или настроить автоматическую очистку

## 🆘 Поддержка

Если возникли проблемы:

1. Проверьте раздел [Troubleshooting](#troubleshooting)
2. Проверьте логи: `logs/control_bot.log` и `logs/automation_*.log`
3. Создайте Issue в репозитории

## 📄 Лицензия

MIT License

## ✅ Чеклист настройки

- [ ] Python 3.9+ установлен
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] Создан .env файл с токенами
- [ ] Получены Telegram API credentials (api_id, api_hash)
- [ ] Создан Control Bot через @BotFather
- [ ] Запущен `setup_db.py` и добавлен первый пользователь
- [ ] Создана сессия в БД
- [ ] Создан файл сессии Telegram
- [ ] Запущен `control_bot.py`
- [ ] Отправлен /start боту в Telegram
- [ ] Сессия запущена и работает

---

**Версия:** 1.0
**Дата:** 2025-11-12
**Автор:** April Shipments Automation Team
