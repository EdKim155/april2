# 🚀 Инструкция по настройке упрощенной автоматизации

## Шаг 1: Проверка зависимостей

### Установка Python библиотек

```bash
# Если есть виртуальное окружение
source venv/bin/activate
pip install telethon

# Или системный Python
pip3 install telethon
```

### Проверка установки

```bash
python3 -c "import telethon; print(telethon.__version__)"
```

Должна отобразиться версия библиотеки (например, `1.34.0`).

---

## Шаг 2: Создание Telegram сессии

**Это нужно сделать ОДИН РАЗ** перед первым запуском автоматизации.

### Запуск скрипта авторизации

```bash
chmod +x create_simple_session.py
python3 create_simple_session.py
```

### Что происходит?

1. Скрипт отправит SMS код на номер `+79512586335`
2. Вы вводите код из SMS
3. Если включена 2FA - вводите пароль
4. Создается файл `simple_automation_session.session`

### Пример вывода

```
======================================================================
🔐 СОЗДАНИЕ TELEGRAM СЕССИИ
======================================================================
📱 Номер телефона: +79512586335
📝 Имя сессии: simple_automation_session.session
======================================================================

🔄 Подключение к Telegram...
📲 Отправка кода авторизации на номер...

──────────────────────────────────────────────────────────────────────
✏️  Введите код из Telegram: 12345
──────────────────────────────────────────────────────────────────────

======================================================================
✅ АВТОРИЗАЦИЯ УСПЕШНА!
======================================================================
👤 Пользователь: Edgar K
🆔 ID: 123456789
📱 Телефон: +79512586335
📁 Файл сессии: simple_automation_session.session
======================================================================

🎉 Сессия создана и сохранена!
Теперь можете запускать ./simple_start.sh
======================================================================
```

---

## Шаг 3: Запуск автоматизации

После успешного создания сессии:

```bash
./simple_start.sh
```

### Проверка работы

```bash
# Просмотр логов в реальном времени
tail -f simple_automation.log

# Проверка процесса
ps aux | grep simple_button_automation
```

---

## Шаг 4: Тестирование

### Ручная проверка

1. Откройте бота `@apri1l_test_bot` в Telegram
2. Дождитесь появления сообщения "Появились новые перевозки"
3. Бот автоматически нажмет на кнопку "🔔Список прямых перевозок"

### Что будет в логах?

```
2025-11-10 12:30:00,123 - __main__ - INFO - ======================================================================
2025-11-10 12:30:00,123 - __main__ - INFO - 🚨 ТРИГГЕР ОБНАРУЖЕН! (#1)
2025-11-10 12:30:00,123 - __main__ - INFO - ======================================================================
2025-11-10 12:30:00,150 - __main__ - INFO - 🎯 Найдена целевая кнопка: "🔔Список прямых перевозок"
2025-11-10 12:30:00,151 - __main__ - INFO - ⚡ Нажатие на кнопку: "🔔Список прямых перевозок"
2025-11-10 12:30:00,345 - __main__ - INFO - ✅ Успешно нажата кнопка: "🔔Список прямых перевозок"
2025-11-10 12:30:00,345 - __main__ - INFO - 🎉 УСПЕШНО!
```

---

## 📋 Полный чеклист

- [ ] Python 3.7+ установлен
- [ ] Библиотека `telethon` установлена
- [ ] Запущен скрипт `create_simple_session.py`
- [ ] Введен SMS код
- [ ] Создан файл `simple_automation_session.session`
- [ ] Запущен скрипт `./simple_start.sh`
- [ ] В логах появилось "🤖 УПРОЩЕННЫЙ БОТ ЗАПУЩЕН"
- [ ] Протестировано на реальном триггере

---

## 🔧 Настройка параметров

### Изменение целевой кнопки

Откройте `simple_button_automation.py`:line_28:

```python
TARGET_BUTTON_TEXT = '🔔Список прямых перевозок'
```

**Важно:** Текст должен **точно совпадать** с текстом кнопки в боте!

### Изменение триггера

Откройте `simple_button_automation.py`:line_25:

```python
TRIGGER_MESSAGE = 'Появились новые перевозки'
```

### Изменение задержки

Откройте `simple_button_automation.py`:line_31:

```python
DELAY_AFTER_TRIGGER = 0.05  # секунды
```

---

## ⚠️ Типичные проблемы

### Проблема: "ModuleNotFoundError: No module named 'telethon'"

**Решение:**
```bash
pip3 install telethon
```

### Проблема: "The session file is invalid"

**Решение:**
```bash
# Удалите старую сессию и создайте заново
rm -f simple_automation_session.session*
python3 create_simple_session.py
```

### Проблема: "Бот не нажимает на кнопку"

**Проверьте:**

1. Точное совпадение текста кнопки:
   ```bash
   grep "TARGET_BUTTON_TEXT" simple_button_automation.py
   ```

2. Логи на наличие ошибок:
   ```bash
   grep -i "error\|ошибка" simple_automation.log
   ```

3. Сохранение клавиатуры:
   ```bash
   grep "Сохранена клавиатура" simple_automation.log
   ```

### Проблема: "Бот не видит триггер"

**Проверьте:**

1. Триггерное сообщение:
   ```bash
   grep "TRIGGER_MESSAGE" simple_button_automation.py
   ```

2. Подключение к боту:
   ```bash
   grep "Подключен к боту" simple_automation.log
   ```

---

## 🔒 Безопасность

### Защита файлов

```bash
# Установите права доступа только для владельца
chmod 600 simple_button_automation.py
chmod 600 simple_automation_session.session*
chmod 600 simple_automation.env
```

### Важные файлы для защиты

- `simple_automation_session.session` - ваша Telegram сессия
- `simple_automation.env` - API ключи
- `simple_button_automation.py` - API credentials в коде

**НЕ ПУБЛИКУЙТЕ** эти файлы в открытом доступе!

---

## 📚 Дополнительные команды

### Статус бота

```bash
# Работает ли бот?
ps aux | grep simple_button_automation

# PID процесса
cat simple_automation.pid
```

### Управление

```bash
# Запуск
./simple_start.sh

# Остановка
./simple_stop.sh

# Перезапуск
./simple_stop.sh && sleep 2 && ./simple_start.sh
```

### Логи

```bash
# Последние 50 строк
tail -50 simple_automation.log

# Поиск ошибок
grep -i "error\|ошибка" simple_automation.log

# Статистика
grep "СТАТИСТИКА" simple_automation.log

# Успешные нажатия
grep "Успешно нажата" simple_automation.log
```

---

## 🎯 Используемые credentials

```
API ID:        24101164
API Hash:      80cc2adcd452008ae630d0ee778b5122
Phone:         +79512586335
Bot:           @apri1l_test_bot
Session:       simple_automation_session.session
```

---

## ✅ Готово!

После выполнения всех шагов бот будет:

1. ✅ Работать в фоновом режиме
2. ✅ Отслеживать триггер "Появились новые перевозки"
3. ✅ Автоматически нажимать на "🔔Список прямых перевозок"
4. ✅ Записывать все действия в лог
5. ✅ Показывать статистику работы

**Вопросы?** Проверьте логи: `tail -f simple_automation.log`
