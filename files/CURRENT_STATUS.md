# 📊 Текущий статус автоматизации на сервере

**Дата обновления:** 2025-11-10 08:11

---

## ✅ Запущенные боты

### 1. Упрощенная автоматизация (только нажатие кнопки)

```
Файл:         /root/simple_automation/simple_button_automation.py
Сервис:       simple-automation.service
Статус:       ✅ Active (running)
Бот:          @ACarriers_bot
Триггер:      "Появились новые перевозки"
Действие:     Нажатие на кнопку "🔔Список прямых перевозок"
Скорость:     ⚡ МГНОВЕННАЯ (~0.05 сек)
PID:          361476
```

**Управление:**
```bash
systemctl status simple-automation
systemctl restart simple-automation
tail -f /root/simple_automation/simple_automation.log
```

---

### 2. Полная автоматизация (полное бронирование)

```
Файл:         /root/automation/bot_automation.py
Процесс:      nohup python3 bot_automation.py
Статус:       ✅ Running
Бот:          @ACarriers_bot
Триггер:      "Появились новые перевозки"
Действие:     Полное бронирование (выбор рейса + подтверждение)
Скорость:     Быстрая (0.05-1.5 сек на все действия)
PID:          362014
```

**Управление:**
```bash
# Статус
ps aux | grep bot_automation | grep -v grep

# Остановка
pkill -f bot_automation.py

# Запуск
cd /root/automation && nohup python3 bot_automation.py > automation.log 2>&1 &

# Логи
tail -f /root/automation/automation.log
```

---

## 🔧 Конфигурация

### Общие параметры:

```
Сервер:       72.56.76.248
SSH ключ:     ~/.ssh/id_ed25519_aprel
API ID:       24101164
Телефон:      +79512586335
```

### Настройки бота:

```
Бот:          @ACarriers_bot
Триггер:      "Появились новые перевозки"
Кнопка:       "🔔Список прямых перевозок"
```

---

## 📝 Файлы конфигурации

### Упрощенная автоматизация:
```
/root/simple_automation/simple_button_automation.py
/root/simple_automation/simple_automation_session.session
```

### Полная автоматизация:
```
/root/automation/bot_automation.py
/root/automation/.env
/root/automation/telegram_session.session
```

---

## 🎯 Различия между ботами

| Параметр | Упрощенная | Полная |
|----------|-----------|--------|
| **Нажатие на "🔔Список перевозок"** | ✅ | ✅ |
| **Выбор конкретного рейса** | ❌ | ✅ |
| **Просмотр деталей** | ❌ | ✅ |
| **Бронирование/подтверждение** | ❌ | ✅ |
| **Скорость реакции** | ⚡ ~0.05 сек | ~0.05 сек |
| **Полное время** | ~0.05 сек | ~0.5-1.5 сек |
| **Системд сервис** | ✅ | ❌ |

---

## 🚀 Рекомендации

### Какой бот использовать?

**Используйте УПРОЩЕННУЮ автоматизацию если:**
- Нужно только открыть список перевозок
- Выбор рейса делается вручную
- Важна максимальная скорость реакции

**Используйте ПОЛНУЮ автоматизацию если:**
- Нужно автоматически бронировать перевозки
- Хотите полную автоматизацию без участия человека
- Готовы к небольшой задержке на весь процесс

### ⚠️ Важно:

**НЕЛЬЗЯ** запускать оба бота одновременно на одном аккаунте!
- Это приведет к конфликтам
- Выберите один бот в зависимости от задачи

**В данный момент запущены ОБА бота** - рекомендую остановить один из них!

---

## 🔄 Переключение между ботами

### Остановить полную автоматизацию, оставить упрощенную:

```bash
ssh -i ~/.ssh/id_ed25519_aprel root@72.56.76.248 "pkill -f bot_automation.py"
```

### Остановить упрощенную, оставить полную:

```bash
ssh -i ~/.ssh/id_ed25519_aprel root@72.56.76.248 "systemctl stop simple-automation"
```

### Запустить только упрощенную:

```bash
ssh -i ~/.ssh/id_ed25519_aprel root@72.56.76.248 "systemctl start simple-automation"
```

### Запустить только полную:

```bash
ssh -i ~/.ssh/id_ed25519_aprel root@72.56.76.248 "cd /root/automation && nohup python3 bot_automation.py > automation.log 2>&1 &"
```

---

## 📊 Мониторинг

### Проверка статуса обоих ботов:

```bash
ssh -i ~/.ssh/id_ed25519_aprel root@72.56.76.248 'echo "=== Упрощенная ===" && systemctl status simple-automation && echo "" && echo "=== Полная ===" && ps aux | grep bot_automation | grep -v grep'
```

### Логи в реальном времени:

**Упрощенная:**
```bash
ssh -i ~/.ssh/id_ed25519_aprel root@72.56.76.248 "tail -f /root/simple_automation/simple_automation.log"
```

**Полная:**
```bash
ssh -i ~/.ssh/id_ed25519_aprel root@72.56.76.248 "tail -f /root/automation/automation.log"
```

---

## 🎉 Готово!

Оба бота настроены на @ACarriers_bot и запущены на сервере.

**Рекомендация:** Определитесь, какой бот вам нужен, и остановите другой, чтобы избежать конфликтов.
