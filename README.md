# Telegram Bot Automation для Бронирования Перевозок

Автоматизация для максимально быстрого бронирования перевозок через Telegram бота. Использует сессию Telegram для автоматического нажатия кнопок при появлении новых перевозок.

## Особенности

- **Максимальная скорость**: Использует сохраненную клавиатуру вместо отправки `/start`, что экономит время
- **Умное нажатие кнопок**: Поддерживает несколько стратегий нажатия (первая кнопка, по ключевым словам, все кнопки)
- **Минимальные задержки**: Настраиваемые задержки от 0.05 секунды
- **Статистика работы**: Отслеживание количества триггеров, нажатых кнопок и ошибок
- **Подробное логирование**: Информация о каждом действии бота

## Принцип работы

1. Бот подключается к вашему аккаунту Telegram через пользовательскую сессию
2. Отслеживает сообщения от указанного бота
3. Сохраняет все клавиатуры с кнопками
4. При появлении триггерного сообщения "Появились новые перевозки":
   - **НЕ отправляет /start** (экономия времени!)
   - Использует последнюю сохраненную клавиатуру
   - Автоматически нажимает нужные кнопки
5. Продолжает мониторинг

## Установка

### 1. Клонируйте репозиторий

```bash
git clone <url>
cd april2
```

### 2. Установите зависимости

```bash
pip install -r requirements.txt
```

### 3. Получите API credentials от Telegram

1. Перейдите на https://my.telegram.org/apps
2. Войдите под своим номером телефона
3. Создайте новое приложение
4. Сохраните `API_ID` и `API_HASH`

### 4. Настройте переменные окружения

Скопируйте файл `.env.example` в `.env`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и укажите свои данные:

```env
API_ID=your_api_id
API_HASH=your_api_hash
PHONE_NUMBER=+79001234567
BOT_USERNAME=@your_bot_username
SESSION_NAME=telegram_session
```

### 5. Настройте конфигурацию (опционально)

Отредактируйте файл `config.py` для изменения параметров работы бота:

```python
CONFIG = {
    # Задержки между действиями (в секундах)
    'DELAY_AFTER_TRIGGER': 0.05,   # Минимальная задержка после обнаружения триггера
    'DELAY_BETWEEN_CLICKS': 0.1,    # Задержка между нажатиями кнопок
    'DELAY_WAIT_RESPONSE': 0.3,     # Ожидание ответа от бота

    # Стратегия нажатия кнопок
    'BUTTON_STRATEGY': 'first',     # 'first', 'all', 'custom'

    # Ключевые слова для поиска кнопок (если BUTTON_STRATEGY = 'custom')
    'BUTTON_KEYWORDS': [
        'Забронировать',
        'Взять заказ',
        'Принять',
    ],
}
```

## Использование

### Первый запуск

При первом запуске вам нужно будет авторизоваться:

```bash
python bot_automation.py
```

Вам придет код подтверждения в Telegram. Введите его в консоль.

### Последующие запуски

После успешной авторизации создастся файл сессии (`telegram_session.session`). При следующих запусках авторизация не потребуется:

```bash
python bot_automation.py
```

### Остановка бота

Нажмите `Ctrl+C` для остановки бота.

## Стратегии нажатия кнопок

### 1. First (Первая кнопка) - По умолчанию

```python
'BUTTON_STRATEGY': 'first'
```

Нажимает на первую доступную кнопку из последнего меню. **Самая быстрая стратегия.**

### 2. Custom (По ключевым словам)

```python
'BUTTON_STRATEGY': 'custom'
'BUTTON_KEYWORDS': ['Забронировать', 'Взять заказ']
```

Ищет кнопку, содержащую одно из ключевых слов. Если не находит, переключается на стратегию 'first'.

### 3. All (Все кнопки)

```python
'BUTTON_STRATEGY': 'all'
```

Нажимает на все кнопки последовательно с заданной задержкой.

## Примеры логов

```
============================================================
🤖 Telegram Bot Automation - ЗАПУЩЕН
============================================================
📱 Триггерное сообщение: 'Появились новые перевозки'
⚡ Стратегия кнопок: 'first'
💨 Режим: МАКСИМАЛЬНО БЫСТРЫЙ (без /start)
⏱️  Задержка после триггера: 0.05с
============================================================

============================================================
🚨 ОБНАРУЖЕНЫ НОВЫЕ ПЕРЕВОЗКИ! (#1)
⏱️  Время: 14:23:45.123
============================================================
💨 Использую сохраненную клавиатуру (БЫСТРЫЙ режим!)
⚡ Нажатие на кнопку: 'Перевозки' (первая доступная, ряд 1)
✓ Кнопка 'Перевозки' успешно нажата
✅ Кнопки успешно нажаты
============================================================
📊 Статистика: Триггеров: 1, Кнопок нажато: 1, Ошибок: 0, Время работы: 0:05:23
```

## Оптимизация скорости

Для максимальной скорости:

1. Минимизируйте задержки в `config.py`:
   ```python
   'DELAY_AFTER_TRIGGER': 0.05,
   'DELAY_BETWEEN_CLICKS': 0.05,
   'DELAY_WAIT_RESPONSE': 0.2,
   ```

2. Используйте стратегию `'first'` для мгновенного нажатия первой кнопки

3. Убедитесь, что бот заранее запущен и клавиатура уже сохранена

## Устранение неполадок

### Ошибка "No module named 'telethon'"

```bash
pip install -r requirements.txt
```

### Ошибка "API_ID и API_HASH должны быть указаны"

Проверьте файл `.env` и убедитесь, что указали правильные значения.

### Бот не нажимает кнопки

1. Убедитесь, что `BOT_USERNAME` указан правильно (с символом `@`)
2. Проверьте, что бот уже отправил вам хотя бы одно сообщение с кнопками
3. Включите режим DEBUG в `config.py`:
   ```python
   'LOG_LEVEL': 'DEBUG'
   ```

### Ошибка при нажатии кнопки

Убедитесь, что ID последнего сообщения актуален. Попробуйте перезапустить бота.

## Безопасность

- **НЕ делитесь** файлом `.env` или `.session` файлами
- **НЕ коммитьте** в git файлы с учетными данными (они добавлены в `.gitignore`)
- Используйте этот инструмент **только** для личных целей
- **Соблюдайте** Terms of Service Telegram

## Структура проекта

```
april2/
├── bot_automation.py    # Основной скрипт бота
├── config.py           # Конфигурация бота
├── requirements.txt    # Зависимости Python
├── .env.example       # Пример файла с переменными окружения
├── .gitignore         # Игнорируемые файлы для git
└── README.md          # Документация
```

## Лицензия

MIT

## Disclaimer

Этот инструмент предназначен только для образовательных целей. Используйте на свой страх и риск. Автор не несет ответственности за любые последствия использования данного ПО.
