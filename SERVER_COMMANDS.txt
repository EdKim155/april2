╔══════════════════════════════════════════════════════════════════════════════╗
║                    СОФТ РАЗВЕРНУТ НА СЕРВЕРЕ!                                ║
║                  Бот: @apri1l_test_bot                                       ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────────────┐
│ ИНФОРМАЦИЯ О СЕРВЕРЕ                                                         │
└──────────────────────────────────────────────────────────────────────────────┘

IP адрес:         72.56.76.248
Пользователь:     root
SSH ключ:         ~/.ssh/id_ed25519_aprel
Директория:       /root/simple_automation/

Подключение:
ssh -i ~/.ssh/id_ed25519_aprel root@72.56.76.248


┌──────────────────────────────────────────────────────────────────────────────┐
│ СТАТУС БОТА                                                                  │
└──────────────────────────────────────────────────────────────────────────────┘

✅ Бот запущен и работает!
✅ Автозапуск включен (запустится после перезагрузки сервера)
✅ Логирование активно

Сервис:           simple-automation.service
Статус:           Active (running)
PID:              355342


┌──────────────────────────────────────────────────────────────────────────────┐
│ КОМАНДЫ УПРАВЛЕНИЯ (выполнять на сервере)                                    │
└──────────────────────────────────────────────────────────────────────────────┘

# Подключиться к серверу
ssh -i ~/.ssh/id_ed25519_aprel root@72.56.76.248

# Статус сервиса
systemctl status simple-automation

# Остановить
systemctl stop simple-automation

# Запустить
systemctl start simple-automation

# Перезапустить
systemctl restart simple-automation

# Отключить автозапуск
systemctl disable simple-automation

# Включить автозапуск
systemctl enable simple-automation


┌──────────────────────────────────────────────────────────────────────────────┐
│ ПРОСМОТР ЛОГОВ                                                               │
└──────────────────────────────────────────────────────────────────────────────┘

# Логи в реальном времени (основной файл)
ssh -i ~/.ssh/id_ed25519_aprel root@72.56.76.248 "tail -f /root/simple_automation/simple_automation.log"

# Последние 50 строк
ssh -i ~/.ssh/id_ed25519_aprel root@72.56.76.248 "tail -50 /root/simple_automation/simple_automation.log"

# Логи systemd
ssh -i ~/.ssh/id_ed25519_aprel root@72.56.76.248 "journalctl -u simple-automation -f"

# Поиск ошибок
ssh -i ~/.ssh/id_ed25519_aprel root@72.56.76.248 "grep -i 'error\|ошибка' /root/simple_automation/simple_automation.log"

# Статистика
ssh -i ~/.ssh/id_ed25519_aprel root@72.56.76.248 "grep 'СТАТИСТИКА' /root/simple_automation/simple_automation.log"


┌──────────────────────────────────────────────────────────────────────────────┐
│ КОНФИГУРАЦИЯ БОТА                                                            │
└──────────────────────────────────────────────────────────────────────────────┘

Триггер:          "Появились новые перевозки"
Целевая кнопка:   "🔔Список прямых перевозок"

API ID:           24101164
Телефон:          +79512586335
Бот:              @apri1l_test_bot


┌──────────────────────────────────────────────────────────────────────────────┐
│ ИСПРАВЛЕННЫЕ ОШИБКИ                                                          │
└──────────────────────────────────────────────────────────────────────────────┘

✅ Исправлена ошибка AttributeError: 'PeerChannel' object has no attribute 'user_id'
   - Добавлена проверка типа peer_id (PeerUser, PeerChat, PeerChannel)

✅ Улучшен поиск кнопок
   - Точное совпадение текста кнопки
   - Гибкий поиск по ключевым словам
   - Логирование всех доступных кнопок при ошибке

✅ Включено логирование клавиатуры
   - Теперь видно все доступные кнопки в логах


┌──────────────────────────────────────────────────────────────────────────────┐
│ ТЕСТИРОВАНИЕ                                                                 │
└──────────────────────────────────────────────────────────────────────────────┘

1. Проверьте логи:
   ssh -i ~/.ssh/id_ed25519_aprel root@72.56.76.248 "tail -f /root/simple_automation/simple_automation.log"

2. При появлении триггера "Появились новые перевозки" должно произойти:
   - Обнаружение триггера: 🚨 ТРИГГЕР ОБНАРУЖЕН!
   - Логирование кнопок: 💾 Сохранена клавиатура с кнопками
   - Поиск кнопки: 🎯 Найдена целевая кнопка
   - Нажатие: ⚡ Нажатие на кнопку
   - Успех: ✅ Успешно нажата кнопка
   - Завершение: 🎉 УСПЕШНО!

3. Если кнопка не найдена:
   - В логах будет список всех доступных кнопок
   - Можно скорректировать TARGET_BUTTON_TEXT в коде


┌──────────────────────────────────────────────────────────────────────────────┐
│ ОБНОВЛЕНИЕ КОДА                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

Если нужно обновить код на сервере:

1. Остановите сервис:
   ssh -i ~/.ssh/id_ed25519_aprel root@72.56.76.248 "systemctl stop simple-automation"

2. Скопируйте новый файл:
   scp -i ~/.ssh/id_ed25519_aprel simple_button_automation.py root@72.56.76.248:/root/simple_automation/

3. Запустите сервис:
   ssh -i ~/.ssh/id_ed25519_aprel root@72.56.76.248 "systemctl start simple-automation"

4. Проверьте логи:
   ssh -i ~/.ssh/id_ed25519_aprel root@72.56.76.248 "tail -f /root/simple_automation/simple_automation.log"


┌──────────────────────────────────────────────────────────────────────────────┐
│ МОНИТОРИНГ                                                                   │
└──────────────────────────────────────────────────────────────────────────────┘

Создайте скрипт мониторинга на сервере:

ssh -i ~/.ssh/id_ed25519_aprel root@72.56.76.248 'cat > /root/simple_automation/monitor.sh << "EOF"
#!/bin/bash
echo "═══════════════════════════════════════════════════════════"
echo "  МОНИТОРИНГ АВТОМАТИЗАЦИИ @apri1l_test_bot"
echo "═══════════════════════════════════════════════════════════"
echo ""
echo "📊 Статус сервиса:"
systemctl is-active simple-automation && echo "  ✅ Запущен" || echo "  ❌ Остановлен"
echo ""
echo "📝 Последние 10 строк логов:"
tail -10 /root/simple_automation/simple_automation.log
echo ""
echo "📈 Статистика:"
grep "СТАТИСТИКА" /root/simple_automation/simple_automation.log | tail -1 || echo "  Нет данных"
echo ""
echo "⚠️  Последние ошибки:"
grep -i "error\|ошибка" /root/simple_automation/simple_automation.log | tail -3 || echo "  ✅ Ошибок нет"
echo ""
echo "═══════════════════════════════════════════════════════════"
EOF
chmod +x /root/simple_automation/monitor.sh'

# Запуск мониторинга:
ssh -i ~/.ssh/id_ed25519_aprel root@72.56.76.248 "/root/simple_automation/monitor.sh"


┌──────────────────────────────────────────────────────────────────────────────┐
│ ЛОКАЛЬНОЕ ТЕСТИРОВАНИЕ                                                       │
└──────────────────────────────────────────────────────────────────────────────┘

Для тестирования локально (на Mac):

1. Остановите бота на сервере:
   ssh -i ~/.ssh/id_ed25519_aprel root@72.56.76.248 "systemctl stop simple-automation"

2. Запустите локально:
   cd /Users/edgark/Desktop/april2
   source venv/bin/activate
   python3 simple_button_automation.py

3. Просмотр логов:
   tail -f simple_automation.log

4. Остановка: Ctrl+C

5. После тестирования запустите на сервере обратно:
   ssh -i ~/.ssh/id_ed25519_aprel root@72.56.76.248 "systemctl start simple-automation"

⚠️  ВАЖНО: Одновременно может работать только один экземпляр бота (либо локально, либо на сервере)!


╔══════════════════════════════════════════════════════════════════════════════╗
║                              ВСЕ ГОТОВО!                                     ║
╚══════════════════════════════════════════════════════════════════════════════╝

Бот успешно развернут на сервере 72.56.76.248 и работает!

Проверьте логи: ssh -i ~/.ssh/id_ed25519_aprel root@72.56.76.248 "tail -f /root/simple_automation/simple_automation.log"
