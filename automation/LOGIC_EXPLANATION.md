# 🧠 ЛОГИКА РАБОТЫ СОФТА АВТОМАТИЗАЦИИ

## 📊 ОБЩАЯ АРХИТЕКТУРА

```
┌─────────────────────────────────────────────────────────────┐
│                   TELEGRAM BOT (@ACarriers_bot)              │
│                           ↓                                  │
│                    Отправляет сообщения                      │
│                           ↓                                  │
│         ┌─────────────────────────────────────┐             │
│         │   СОФТ АВТОМАТИЗАЦИИ (bot_automation.py)          │
│         │                                     │              │
│         │  1. Слушает ВСЕ сообщения          │              │
│         │  2. Сохраняет клавиатуры            │              │
│         │  3. Ищет триггер                    │              │
│         │  4. Нажимает кнопки автоматически   │              │
│         └─────────────────────────────────────┘             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 ПОШАГОВАЯ ЛОГИКА РАБОТЫ

### ШАГ 1: ИНИЦИАЛИЗАЦИЯ (при запуске)

```python
1. Подключение к Telegram через API
   - Использует API_ID, API_HASH из .env
   - Использует сохраненную сессию (telegram_session.session)

2. Подключение к целевому боту
   - Находит бота @ACarriers_bot по username
   - Сохраняет bot_entity для фильтрации сообщений

3. Регистрация обработчиков
   - Слушает НОВЫЕ сообщения (NewMessage)
   - Слушает ОТРЕДАКТИРОВАННЫЕ сообщения (MessageEdited)
```

---

### ШАГ 2: ОБРАБОТКА ВСЕХ ВХОДЯЩИХ СООБЩЕНИЙ

```python
async def handle_message(message):
    # 1. ФИЛЬТРАЦИЯ: Проверяем отправителя
    if message.peer_id.user_id != bot_entity.id:
        return  # ❌ Сообщение не от @ACarriers_bot - игнорируем
    
    # ✅ Сообщение от @ACarriers_bot - обрабатываем
    
    # 2. СОХРАНЕНИЕ КЛАВИАТУРЫ
    await save_keyboard(message)
    
    # 3. ПРОВЕРКА ТРИГГЕРА
    if TRIGGER_MESSAGE in message.text:
        # 🎯 ТРИГГЕР ОБНАРУЖЕН!
        await process_new_transport(message)
        return
    
    # 4. ПРОДОЛЖЕНИЕ АВТОМАТИЗАЦИИ (если запущена)
    if automation_state:
        await continue_automation(message)
```

**Важно:** Бот обрабатывает как НОВЫЕ, так и ОТРЕДАКТИРОВАННЫЕ сообщения!
- Некоторые боты редактируют (edit) сообщения вместо отправки новых
- Наш бот ловит оба события

---

### ШАГ 3: СОХРАНЕНИЕ КЛАВИАТУРЫ

```python
async def save_keyboard(message):
    # Проверяем, есть ли в сообщении клавиатура
    if message.reply_markup and isinstance(message.reply_markup, ReplyInlineMarkup):
        
        # Сохраняем клавиатуру для последующего использования
        self.last_keyboard = message.reply_markup
        self.last_message_id = message.id
        
        # Логируем кнопки для отладки
        logger.info(f"Сохранена клавиатура с {len(rows)} рядами кнопок")
        
        # Пример вывода:
        # Ряд 1: ['🔔 Список прямых перевозок']
        # Ряд 2: ['📋 Список магистральных перевозок']
```

**Как сохраняется:**
- `last_keyboard` - объект клавиатуры с кнопками
- `last_message_id` - ID сообщения (нужен для нажатия кнопок)

**Структура клавиатуры:**
```
ReplyInlineMarkup
├── rows (массив рядов)
│   ├── Row 1
│   │   ├── Button 1: {text: "🔔 Список прямых перевозок", data: "callback_data_1"}
│   ├── Row 2
│   │   ├── Button 1: {text: "📋 Список магистральных", data: "callback_data_2"}
```

---

### ШАГ 4: ОБНАРУЖЕНИЕ ТРИГГЕРА

```python
# В конфигурации (.env):
TRIGGER_MESSAGE = "Появились новые перевозки"

# В коде:
if message.text and TRIGGER_MESSAGE in message.text:
    # 🎯 ТРИГГЕР ОБНАРУЖЕН!
    await process_new_transport(message)
```

**Как работает:**
- Бот проверяет КАЖДОЕ сообщение от @ACarriers_bot
- Ищет подстроку `TRIGGER_MESSAGE` в тексте сообщения
- Использует оператор `in` - поиск подстроки (не точное совпадение!)

**Примеры срабатывания:**
```
✅ "Появились новые перевозки"           → СРАБОТАЕТ
✅ "🚨 Появились новые перевозки!"        → СРАБОТАЕТ
✅ "Внимание! Появились новые перевозки" → СРАБОТАЕТ
❌ "Новая перевозка доступна"            → НЕ СРАБОТАЕТ
```

---

### ШАГ 5: ВЫБОР КНОПОК (3 СТРАТЕГИИ)

После обнаружения триггера запускается `click_buttons_by_strategy()`:

#### 🎯 СТРАТЕГИЯ 1: "first" (по умолчанию)

```python
if strategy == 'first':
    # Проходим по всем рядам клавиатуры
    for row_idx, row in enumerate(last_keyboard.rows):
        # Проходим по всем кнопкам в ряду
        for btn_idx, button in enumerate(row.buttons):
            # Находим первую callback-кнопку
            if isinstance(button, KeyboardButtonCallback):
                # НАЖИМАЕМ ЕЁ!
                return await click_button(button)
```

**Логика:**
1. Берет ПЕРВЫЙ ряд клавиатуры
2. Берет ПЕРВУЮ кнопку в этом ряду
3. Проверяет, что это callback-кнопка (не обычная текстовая)
4. Нажимает её

**Пример для @ACarriers_bot:**
```
Ряд 1: ['🔔 Список прямых перевозок'] ← НАЖМЁТ ЭТУ
Ряд 2: ['📋 Список магистральных перевозок']
Ряд 3: ['📋 Мои перевозки']
```

#### 🎯 СТРАТЕГИЯ 2: "custom" (по ключевым словам)

```python
if strategy == 'custom':
    keywords = CONFIG.get('BUTTON_KEYWORDS', [])  # например: ['магистральн']
    
    # Ищем кнопку, содержащую любое из ключевых слов
    for row in last_keyboard.rows:
        for button in row.buttons:
            for keyword in keywords:
                if keyword.lower() in button.text.lower():
                    # НАШЛИ! Нажимаем
                    return await click_button(button)
    
    # Если не нашли - fallback на 'first'
    logger.warning("Не найдено кнопок с ключевыми словами")
    # Переключаемся на стратегию 'first'
```

**Логика:**
1. Берет список ключевых слов из конфигурации
2. Проходит по ВСЕМ кнопкам
3. Ищет первую кнопку, которая СОДЕРЖИТ любое ключевое слово
4. Нажимает её
5. Если не нашел - нажимает первую доступную

**Пример:**
```python
# Конфигурация:
'BUTTON_KEYWORDS': ['магистральн', 'магистраль']

# Клавиатура:
Ряд 1: ['🔔 Список прямых перевозок']
Ряд 2: ['📋 Список магистральных перевозок'] ← НАЖМЁТ ЭТУ (содержит "магистральн")
Ряд 3: ['📋 Мои перевозки']
```

#### 🎯 СТРАТЕГИЯ 3: "all" (все кнопки)

```python
if strategy == 'all':
    # Нажимаем ВСЕ кнопки по очереди
    for row in last_keyboard.rows:
        for button in row.buttons:
            if isinstance(button, KeyboardButtonCallback):
                await click_button(button)
                # Ждем между нажатиями
                await asyncio.sleep(delay)  # 0.05-0.15 секунд
```

**Логика:**
1. Проходит по ВСЕМ рядам
2. Проходит по ВСЕМ кнопкам
3. Нажимает КАЖДУЮ callback-кнопку
4. Делает паузу между нажатиями

**Пример для @ACarriers_bot:**
```
Нажмёт:
1. 🔔 Список прямых перевозок
   ⏱️ пауза 0.05-0.15с
2. 📋 Список магистральных перевозок
   ⏱️ пауза 0.05-0.15с
3. 📋 Мои перевозки
   ⏱️ пауза 0.05-0.15с
4. ⚙️ Настройки
```

---

### ШАГ 6: МНОГОШАГОВАЯ АВТОМАТИЗАЦИЯ (STATE MACHINE)

После первого нажатия кнопки запускается **автоматическая цепочка действий**:

```python
# Состояния:
automation_state = None               # Не активна
automation_state = 'waiting_list'     # Ждем список перевозок
automation_state = 'waiting_details'  # Ждем детали перевозки
```

#### ПОСЛЕДОВАТЕЛЬНОСТЬ ДЕЙСТВИЙ:

```
🎯 ТРИГГЕР: "Появились новые перевозки"
    ↓
    ↓ automation_state = 'waiting_list'
    ↓
[Шаг 1] Нажать "🔔 Список прямых перевозок"
    ↓
    ↓ Бот отправляет список перевозок
    ↓ (новое сообщение с кнопками перевозок)
    ↓
[Шаг 2] automation_state == 'waiting_list'
    ↓   → Выбрать ПЕРВУЮ перевозку из списка
    ↓   → Нажать на неё
    ↓   → automation_state = 'waiting_details'
    ↓
    ↓ Бот отправляет детали перевозки
    ↓ (новое сообщение с кнопкой "Забронировать")
    ↓
[Шаг 3] automation_state == 'waiting_details'
    ↓   → Искать кнопку с текстом:
    ↓      - "подтвердить" ИЛИ
    ↓      - "забронировать" ИЛИ
    ↓      - "взять"
    ↓   → Нажать на неё
    ↓   → automation_state = None (завершено!)
    ↓
✅ АВТОМАТИЧЕСКОЕ БРОНИРОВАНИЕ ЗАВЕРШЕНО!
```

#### КОД ЛОГИКИ:

```python
async def continue_automation(message):
    if automation_state == 'waiting_list':
        # ШАГ 2: Получили список перевозок
        logger.info("📋 Получен список перевозок, выбираю первую...")
        
        # Берем ПЕРВУЮ кнопку из списка
        for row in last_keyboard.rows:
            for button in row.buttons:
                if isinstance(button, KeyboardButtonCallback):
                    # Это первая перевозка - нажимаем!
                    await click_button(button, "(первая перевозка)")
                    automation_state = 'waiting_details'
                    return  # Выходим, ждем следующего сообщения
    
    elif automation_state == 'waiting_details':
        # ШАГ 3: Получили детали перевозки
        logger.info("📦 Получены детали, ищу кнопку подтверждения...")
        
        # Ищем кнопку подтверждения
        keywords = ['подтвердить', 'забронировать', 'взять']
        for row in last_keyboard.rows:
            for button in row.buttons:
                for keyword in keywords:
                    if keyword in button.text.lower():
                        # НАШЛИ кнопку бронирования - нажимаем!
                        await click_button(button, "(БРОНИРОВАНИЕ)")
                        automation_state = None  # Завершаем
                        logger.info("🎉 АВТОМАТИЧЕСКОЕ БРОНИРОВАНИЕ ЗАВЕРШЕНО!")
                        return
```

---

### ШАГ 7: НАЖАТИЕ КНОПКИ (техническая реализация)

```python
async def click_button(button, button_info=""):
    # Отправляем callback запрос к Telegram API
    await client(
        functions.messages.GetBotCallbackAnswerRequest(
            peer=bot_entity,              # @ACarriers_bot
            msg_id=last_message_id,       # ID сообщения с кнопкой
            data=button.data              # callback_data кнопки
        )
    )
```

**Что происходит:**
1. Берет `callback_data` из кнопки (уникальный идентификатор)
2. Отправляет запрос боту: "Я нажал кнопку X в сообщении Y"
3. Бот получает callback и обрабатывает (как будто пользователь нажал)

---

## 🔍 ФИЛЬТРАЦИЯ СООБЩЕНИЙ

Бот игнорирует сообщения от других ботов/пользователей:

```python
# Проверка отправителя
if message.peer_id.user_id != bot_entity.id:
    return  # Игнорируем
```

**Обрабатываются ТОЛЬКО:**
- Сообщения от @ACarriers_bot (bot_entity.id)
- Новые сообщения (NewMessage)
- Отредактированные сообщения (MessageEdited)

---

## ⏱️ ЗАДЕРЖКИ И ТАЙМИНГ

```python
CONFIG = {
    'DELAY_AFTER_TRIGGER': 0.001,      # После обнаружения триггера
    'DELAY_BETWEEN_CLICKS': 0.05,       # Между нажатиями (стратегия 'all')
    'KEYBOARD_UPDATE_TIMEOUT': 0.3,     # Таймаут ожидания обновления
}
```

**Почему нужны задержки:**
1. **DELAY_AFTER_TRIGGER**: Дать время боту обработать предыдущее сообщение
2. **DELAY_BETWEEN_CLICKS**: Telegram может ограничивать частоту запросов
3. **KEYBOARD_UPDATE_TIMEOUT**: Дать время боту обновить клавиатуру

---

## 📊 ИТОГОВАЯ СХЕМА ПРИНЯТИЯ РЕШЕНИЙ

```
[ВХОДЯЩЕЕ СООБЩЕНИЕ]
    ↓
[ОТ @ACarriers_bot?]
    ├─ НЕТ → ❌ ИГНОРИРУЕМ
    └─ ДА → ✅ ОБРАБАТЫВАЕМ
        ↓
[СОХРАНЯЕМ КЛАВИАТУРУ]
    ↓
[ЕСТЬ ТРИГГЕР В ТЕКСТЕ?]
    ├─ ДА → 🎯 ЗАПУСКАЕМ АВТОМАТИЗАЦИЮ
    │       ↓
    │   [СТРАТЕГИЯ = ?]
    │       ├─ "first" → Нажать ПЕРВУЮ кнопку
    │       ├─ "custom" → Найти по КЛЮЧЕВЫМ СЛОВАМ
    │       └─ "all" → Нажать ВСЕ кнопки
    │       ↓
    │   [ЗАПУСКАЕМ STATE MACHINE]
    │       ↓
    │   [ЖДЕМ СЛЕДУЮЩИХ СООБЩЕНИЙ ДЛЯ ПРОДОЛЖЕНИЯ]
    │
    └─ НЕТ → [АВТОМАТИЗАЦИЯ ЗАПУЩЕНА?]
            ├─ ДА → ПРОДОЛЖАЕМ (continue_automation)
            └─ НЕТ → ЖДЕМ ТРИГГЕРА
```

---

## 🎛️ НАСТРОЙКА ПОД РАЗНЫЕ БОТЫ

### Изменить целевого бота:
```bash
# В .env:
BOT_USERNAME=@YourBot
```

### Изменить триггер:
```bash
# В .env:
TRIGGER_MESSAGE=Ваш текст триггера
```

### Изменить стратегию:
```python
# В config.py:
'BUTTON_STRATEGY': 'first'  # или 'custom' или 'all'
'BUTTON_KEYWORDS': ['нужное', 'слово']  # для 'custom'
```

---

## 📝 ПРИМЕРЫ ЛОГОВ

### Успешная автоматизация:
```
07:50:54 - 🤖 Telegram Bot Automation - ЗАПУЩЕН
07:50:54 - 📱 Триггерное сообщение: 'Появились новые перевозки'
07:50:54 - ⚡ Стратегия кнопок: 'first'

[Приходит сообщение от бота]
08:15:23 - Сохранена клавиатура с 4 рядами кнопок
08:15:23 -   Ряд 1: ['🔔 Список прямых перевозок']
08:15:23 -   Ряд 2: ['📋 Список магистральных перевозок']

[Приходит триггерное сообщение]
08:16:45 - ============================================================
08:16:45 - 🚨 ОБНАРУЖЕНЫ НОВЫЕ ПЕРЕВОЗКИ! (#1)
08:16:45 - ============================================================
08:16:45 - 💨 Использую сохраненную клавиатуру (БЫСТРЫЙ режим!)
08:16:45 - 🤖 Запускаю многошаговую автоматизацию...
08:16:45 - ⚡ Нажатие на кнопку: '🔔 Список прямых перевозок' (первая доступная, ряд 1)
08:16:45 - ✓ Кнопка успешно нажата
08:16:45 - ✅ Шаг 1/3: Открываю список перевозок

[Приходит список перевозок]
08:16:46 - 🔄 Состояние: waiting_list
08:16:46 - 📋 Получен список перевозок, выбираю первую...
08:16:46 - ⚡ Выбираю перевозку: 'Москва → СПб, 20т'
08:16:46 - ✓ Кнопка успешно нажата

[Приходят детали]
08:16:47 - 🔄 Состояние: waiting_details
08:16:47 - 📦 Получены детали, ищу кнопку подтверждения...
08:16:47 - ✅ Нажимаю: 'Забронировать'
08:16:47 - ✓ Кнопка успешно нажата
08:16:47 - 🎉 АВТОМАТИЧЕСКОЕ БРОНИРОВАНИЕ ЗАВЕРШЕНО!

08:16:47 - 📊 Статистика: Триггеров: 1, Кнопок нажато: 3, Ошибок: 0
```

---

## ❓ FAQ

**Q: Почему бот не нажимает кнопку?**
A: Проверьте:
1. Сохранена ли клавиатура? (смотрите логи "Сохранена клавиатура")
2. Правильная ли стратегия?
3. Для 'custom': есть ли ключевое слово в тексте кнопки?

**Q: Как узнать, какие кнопки доступны?**
A: Смотрите логи с DEBUG уровнем - там выводятся все ряды и кнопки

**Q: Можно ли нажать конкретную кнопку, например, вторую?**
A: Да, используйте стратегию 'custom' с ключевым словом из названия кнопки

**Q: Почему бот нажимает не ту кнопку?**
A: Стратегия 'first' всегда нажимает ПЕРВУЮ. Используйте 'custom' для выбора конкретной.

**Q: Бот нажимает кнопку, но ничего не происходит?**
A: Проверьте, что сохранен правильный `last_message_id` - кнопки работают только для своего сообщения.
