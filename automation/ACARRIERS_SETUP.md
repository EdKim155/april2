# ⚙️ Настройка для @ACarriers_bot

## ✅ Текущая конфигурация

**Бот:** @ACarriers_bot  
**Триггерное сообщение:** "Появились новые перевозки"  
**Стратегия:** Первая кнопка (🔔 Список прямых перевозок)  
**Статус:** ✅ РАБОТАЕТ (PID: проверьте через `ps aux | grep bot_automation`)

## 🔧 Файлы конфигурации

### /root/automation/.env
```bash
BOT_USERNAME=@ACarriers_bot
TRIGGER_MESSAGE=Появились новые перевозки
```

### /root/automation/config.py
```python
CONFIG = {
    'DELAY_AFTER_TRIGGER': 0.001,      # Задержка после триггера
    'DELAY_BETWEEN_CLICKS': 0.05,       # Задержка между кликами
    'BUTTON_STRATEGY': 'first',         # first/all/custom
    'LOG_LEVEL': 'DEBUG',               # DEBUG/INFO/WARNING/ERROR
}
```

## 📋 Доступные кнопки в @ACarriers_bot

Бот сохранил следующую клавиатуру:

1. 🔔 **Список прямых перевозок** ← По умолчанию нажимается эта
2. 📋 **Список магистральных перевозок**
3. 📋 **Мои перевозки**
4. ⚙️ **Настройки**

## 🎯 Как работает автоматизация

1. Бот слушает все сообщения от @ACarriers_bot
2. При получении сообщения **"Появились новые перевозки"**:
   - Нажимает кнопку "🔔 Список прямых перевозок"
   - Выбирает первую перевозку
   - Подтверждает бронирование
3. Все действия логируются в `/root/automation/automation.log`

## 🔄 Изменение стратегии кнопок

### Вариант 1: Нажимать кнопку "Магистральные перевозки"

```bash
# Изменить в config.py:
'BUTTON_STRATEGY': 'custom',
'BUTTON_KEYWORDS': ['магистральн', 'магистраль'],
```

### Вариант 2: Нажимать все кнопки подряд

```bash
# Изменить в config.py:
'BUTTON_STRATEGY': 'all',
```

### Вариант 3: Только прямые перевозки (текущая)

```bash
# Изменить в config.py:
'BUTTON_STRATEGY': 'first',  # уже установлено
```

## 📝 Изменение триггерного сообщения

Если @ACarriers_bot отправляет другое сообщение, измените в `.env`:

```bash
# Редактировать файл
nano /root/automation/.env

# Изменить строку:
TRIGGER_MESSAGE=Ваш текст триггера

# Или частичное совпадение:
TRIGGER_MESSAGE=новые перевозки  # Будет реагировать на любое сообщение содержащее эту фразу
```

## 🚀 Команды управления

```bash
# Просмотр логов в реальном времени
ssh -i ~/.ssh/id_ed25519_aprel root@72.56.76.248 "tail -f /root/automation/automation.log"

# Проверка статуса
ssh -i ~/.ssh/id_ed25519_aprel root@72.56.76.248 "ps aux | grep bot_automation"

# Перезапуск после изменения настроек
ssh -i ~/.ssh/id_ed25519_aprel root@72.56.76.248 "cd /root/automation && ./stop_automation.sh && sleep 2 && ./start_automation.sh"

# Остановка
ssh -i ~/.ssh/id_ed25519_aprel root@72.56.76.248 "cd /root/automation && ./stop_automation.sh"

# Запуск
ssh -i ~/.ssh/id_ed25519_aprel root@72.56.76.248 "cd /root/automation && ./start_automation.sh"
```

## 🔍 Мониторинг работы

```bash
# Найти триггеры (когда обнаружены новые перевозки)
ssh -i ~/.ssh/id_ed25519_aprel root@72.56.76.248 "grep 'ОБНАРУЖЕНЫ НОВЫЕ ПЕРЕВОЗКИ' /root/automation/automation.log"

# Найти успешные нажатия
ssh -i ~/.ssh/id_ed25519_aprel root@72.56.76.248 "grep 'успешно нажата' /root/automation/automation.log"

# Статистика работы
ssh -i ~/.ssh/id_ed25519_aprel root@72.56.76.248 "grep 'Статистика' /root/automation/automation.log | tail -10"

# Ошибки
ssh -i ~/.ssh/id_ed25519_aprel root@72.56.76.248 "grep -i error /root/automation/automation.log"
```

## ⚠️ Важные моменты

1. **Триггерное сообщение**: Убедитесь, что @ACarriers_bot действительно отправляет сообщение "Появились новые перевозки". Если текст другой - измените в `.env`

2. **Стратегия кнопок**: Сейчас установлена "first" - нажимается первая кнопка. Если нужна другая кнопка - измените стратегию.

3. **Логи**: Всегда проверяйте логи для диагностики:
   ```bash
   tail -50 /root/automation/automation.log
   ```

4. **Перезапуск после изменений**: После любого изменения конфигурации нужно перезапустить бота:
   ```bash
   cd /root/automation
   ./stop_automation.sh
   ./start_automation.sh
   ```

## 📊 Пример логов успешной работы

```
2025-11-06 07:50:54,249 - 🤖 Telegram Bot Automation - ЗАПУЩЕН
2025-11-06 07:50:54,249 - 📱 Триггерное сообщение: 'Появились новые перевозки'
2025-11-06 07:50:54,249 - ⚡ Стратегия кнопок: 'first'
2025-11-06 08:15:23,123 - 🚨 ОБНАРУЖЕНЫ НОВЫЕ ПЕРЕВОЗКИ\!
2025-11-06 08:15:23,145 - ⚡ Нажатие на кнопку: '🔔 Список прямых перевозок'
2025-11-06 08:15:23,289 - ✓ Кнопка успешно нажата
```
